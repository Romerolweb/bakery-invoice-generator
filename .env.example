# Environment Variables Template
# Copy this file to .env.dev or .env.prod and modify as needed

# Server Configuration
PORT=9002                    # Server port (9002 for dev, 3000 for prod)
NODE_ENV=development         # Environment mode (development/production)

# PDF Generation
PDF_GENERATOR=pdfkit        # PDF generation engine (currently supports: pdfkit)

# Debug (optional)
# DEBUG=*                   # Enable debug logging (uncomment for verbose logs)

# Add other project-specific variables below
# DATABASE_URL=             # Database connection string
# API_KEY=                  # External API key
# SECRET_KEY=               # Application secret key
